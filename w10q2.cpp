#include<bits/stdc++.h>
using namespace std;
    
int main(){
    int n;
    cin >> n;
    vector<int> activity(n);
    vector<int> deadline(n);

    for(int i=0; i<n ; i++){
        cin >> activity[i];
        cin >> deadline[i];
    }

    

    return 0;
}